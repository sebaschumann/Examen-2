<%= form_with(model: medical_center_type) do |form| %>
  <% if medical_center_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(medical_center_type.errors.count, "error") %> prohibited this medical_center_type from being saved:</h2>

      <ul>
        <% medical_center_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
